machine:
    python:
        version: 3.4.3
    environment:
        PYTEST_ADDOPTS: --cov=py3template --cov-report html --cov-report term

test:
    override:
        - py.test 

dependencies:
    pre:
       -  pip install pytest

